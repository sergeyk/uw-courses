#departments-list.span-12.prepend-4.append-4.center
  %h2 Listing all departments
  %table
    %tr.header-row
      %td Department Abbreviation
      %td Number of evaluations
      %td Average Rating
      - @departments.each do |department|
        %tr{:class => cycle('even-row','odd-row')}
          %td= link_to(department.human_name, department_path(department))
          %td TODO
          %td TODO